export enum TokenType {
    RESOURCE = "RESOURCE",
    RELATION = "RELATION",
    PERMISSION = "PERMISSION",

    AND = "AND",
    OR = "OR",
    NOT = "NOT",

    IDENTIFIER = "IDENTIFIER",

    LBRACE = "LBRACE",
    RBRACE = "RBRACE",
    LPAREN = "LPAREN",
    RPAREN = "RPAREN",

    EQUALS = "EQUALS",
    ARROW = "ARROW",

    EOF = "EOF"
}
