info:
  name: Insert Resources
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:7077/api/resources
  body:
    type: json
    data: |-
      {
        "type":"doctor",
        "name":"doctor_101"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
