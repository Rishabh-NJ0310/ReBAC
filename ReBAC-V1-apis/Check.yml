info:
  name: Check
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:7077/api/check?subjectId=1&resourceId=2&relation=assigned_to
  params:
    - name: subjectId
      value: "1"
      type: query
    - name: resourceId
      value: "2"
      type: query
    - name: relation
      value: assigned_to
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
