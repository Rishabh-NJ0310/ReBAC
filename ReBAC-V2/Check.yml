info:
  name: Check
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:7077/api/check?subjectId=3&resourceId=1&relation=assigned_to
  params:
    - name: subjectId
      value: "3"
      type: query
    - name: resourceId
      value: "1"
      type: query
    - name: relation
      value: assigned_to
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
