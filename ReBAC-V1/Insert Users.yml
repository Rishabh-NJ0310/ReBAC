info:
  name: Insert Users
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:7077/api/users
  body:
    type: json
    data: |-
      {
        "name":"Aditi",
        "email":"aditi-email@gmail.com",
        "password":"raj10"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
