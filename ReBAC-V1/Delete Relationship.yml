info:
  name: Delete Relationship
  type: http
  seq: 10

http:
  method: DELETE
  url: http://localhost:7077/api/relationships/?id=1
  params:
    - name: id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
